<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
			<%= csrf_meta_tag %>
          <meta name ="viewport" content="width=device-width ,initial-scale=1" >

		<%= render 'layouts/stylesheets' %>

	</head>
<body>
			<%= yield %>
</body>
<%= debug(params) if Rails.env.development? %>

<%= render 'layouts/jquery' %>
</html>
